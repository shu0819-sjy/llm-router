# Pinned direct runtime dependencies for reproducible installs / Docker builds.
#
# Reproducibility policy (v0.3):
# - Direct dependencies are exact-pinned (==) and only moved deliberately.
# - The Docker base image is digest-pinned (see Dockerfile) and installs from
#   this file, so image contents vary only with these pins and that digest.
# - Transitive dependencies are not hash-locked; treat this as direct-pin
#   level reproducibility, not bit-for-bit. Refresh procedure: bump the pin,
#   `pip install -r requirements.txt`, then re-run the test suite, the Docker
#   build, and scripts/restart_migration_smoke.py. See CONTRIBUTING.md.
fastapi==0.133.1
uvicorn[standard]==0.41.0
httpx==0.28.1
pydantic==2.13.4
pydantic-settings==2.13.1
python-dotenv==1.2.2
aiosqlite==0.22.1
